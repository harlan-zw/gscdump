diff --git a/src/catalog/rest.js b/src/catalog/rest.js
index 723451f34f95dab280ac699abdfd34c9703bba50..4bd3e8bf25f7da11a732b8493b89ae9e5606b6de 100644
--- a/src/catalog/rest.js
+++ b/src/catalog/rest.js
@@ -173,7 +173,7 @@ export async function restCatalogCreateTable(ctx, {
   const res = await restFetch(ctx, `namespaces/${ns}/tables`, {
     method: 'POST',
     headers: { 'content-type': 'application/json' },
-    body: JSON.stringify(body),
+    body: stringifyIcebergJson(body),
   })
   const responseBody = parseIcebergJson(await res.text())
   return {
@@ -205,7 +205,7 @@ export async function restCatalogRegisterTable(ctx, { namespace, table, metadata
   const res = await restFetch(ctx, `namespaces/${ns}/register`, {
     method: 'POST',
     headers: { 'content-type': 'application/json' },
-    body: JSON.stringify(body),
+    body: stringifyIcebergJson(body),
   })
   const responseBody = parseIcebergJson(await res.text())
   return {
@@ -280,7 +280,7 @@ export async function restCatalogCreateNamespace(ctx, { namespace, properties })
   const res = await restFetch(ctx, 'namespaces', {
     method: 'POST',
     headers: { 'content-type': 'application/json' },
-    body: JSON.stringify({ namespace: ns, properties: properties ?? {} }),
+    body: stringifyIcebergJson({ namespace: ns, properties: properties ?? {} }),
   })
   const body = parseIcebergJson(await res.text())
   return {
@@ -316,7 +316,7 @@ export async function restCatalogRenameTable(ctx, { source, destination }) {
   await restFetch(ctx, 'tables/rename', {
     method: 'POST',
     headers: { 'content-type': 'application/json' },
-    body: JSON.stringify({ source, destination }),
+    body: stringifyIcebergJson({ source, destination }),
   })
 }
 
diff --git a/src/index.js b/src/index.js
index 2bac90d09cfa5a05243b3cf8d450df2f9e4f3c3a..a177d71fca3a3dc840b8fadfaa3ca3d7dfe8eac6 100644
--- a/src/index.js
+++ b/src/index.js
@@ -1,4 +1,4 @@
-export { IcebergTransactionConflictError, icebergAppend, icebergCreateTable, icebergDelete, icebergDropTable, icebergExpireSnapshots, icebergRewrite, icebergSetRef, icebergTransaction, icebergUpdateSchema } from './write/write.js'
+export { IcebergTransactionConflictError, icebergAppend, icebergAppendBatches, icebergCreateTable, icebergDelete, icebergDropTable, icebergExpireSnapshots, icebergRewrite, icebergSetRef, icebergTransaction, icebergUpdateSchema } from './write/write.js'
 export { icebergCreate } from './create.js'
 export { fileCatalog } from './catalog/file.js'
 export { restCatalogConnect, restCatalogCreateNamespace, restCatalogDropNamespace, restCatalogListNamespaces, restCatalogListTables, restCatalogLoadCredentials, restCatalogLoadTable, restCatalogRegisterTable, restCatalogRenameTable } from './catalog/rest.js'
diff --git a/src/manifest.js b/src/manifest.js
index ba5a83e72ab90761f17bdfa1c3e876014f7b77c5..a6018c2d4b64c47c6fb9a597b7148f20fcf84574 100644
--- a/src/manifest.js
+++ b/src/manifest.js
@@ -7,13 +7,21 @@ import { fetchAvroRecords, urlResolver } from './fetch.js'
  *
  * @import {Resolver, TableMetadata, Manifest, ManifestEntry} from '../src/types.js'
  * @typedef {{ url: string, entries: ManifestEntry[] }[]} ManifestList
+ * @typedef {(partitions: Manifest['partitions'], partitionSpecId: number, manifest: Manifest) => boolean} ManifestPartitionFilter
  * @param {object} options
  * @param {TableMetadata} options.metadata
  * @param {Resolver} [options.resolver]
  * @param {number | bigint} [options.snapshotId] - Optional snapshot id; defaults to `current-snapshot-id`.
+ * @param {ManifestPartitionFilter} [options.partitionFilter] - Optional predicate
+ *   applied to each manifest's manifest-list `partitions` field-summary array
+ *   BEFORE `fetchManifests` fans out. Return `false` to skip fetching that
+ *   manifest's entries entirely (the entry walk is the expensive part). Stays
+ *   generic: the caller owns the partition-spec decode and bounds logic. A
+ *   manifest is always kept when the predicate throws or returns a non-`false`
+ *   value, so a keep-all fallback is the safe default.
  * @returns {Promise<ManifestList>}
  */
-export async function icebergManifests({ metadata, resolver, snapshotId }) {
+export async function icebergManifests({ metadata, resolver, snapshotId, partitionFilter }) {
   resolver ??= urlResolver()
   const rawTarget = snapshotId ?? metadata['current-snapshot-id']
   if (rawTarget == null || rawTarget < 0) {
@@ -42,53 +50,102 @@ export async function icebergManifests({ metadata, resolver, snapshotId }) {
     throw new Error('No manifest information found in snapshot')
   }
 
+  // Prune the manifest list using the manifest-list `partitions` field-summary
+  // bounds BEFORE fetching entries. The summaries are already in-hand here (we
+  // just parsed them from the manifest-list avro), so a partition-pruning
+  // caller pays zero extra fetches to skip a manifest. Keep-all on throw so a
+  // buggy predicate degrades to the unpruned walk rather than dropping data.
+  if (partitionFilter) {
+    manifests = manifests.filter((manifest) => {
+      let keep = true
+      try {
+        keep = partitionFilter(manifest.partitions, manifest.partition_spec_id ?? 0, manifest) !== false
+      } catch {
+        keep = true
+      }
+      return keep
+    })
+  }
+
   return await fetchManifests(manifests, resolver)
 }
 
+// gscdump patch: bound the manifest-entry fetch fan-out. The multi-tenant
+// global fact tables can have hundreds of manifests in one snapshot; an
+// unbounded `Promise.all` opened one in-flight fetch + one buffered avro per
+// manifest at once, which blows the Cloudflare Workers subrequest ceiling and
+// spikes isolate memory on a wide read. A fixed-size worker pool caps in-flight
+// fetches while preserving input order in the result.
+const MANIFEST_FETCH_CONCURRENCY = 8
+
 /**
- * Fetch manifest entries from a list of manifests in parallel.
+ * Fetch one manifest's entries and apply sequence-number / first-row-id
+ * inheritance. Extracted from {@link fetchManifests} so the bounded pool can
+ * call it per manifest.
  *
- * @param {Manifest[]} manifests
+ * @param {Manifest} manifest
  * @param {Resolver} resolver
- * @returns {Promise<ManifestList>}
+ * @returns {Promise<{ url: string, entries: ManifestEntry[] }>}
  */
-async function fetchManifests(manifests, resolver) {
-  // Fetch manifest entries in parallel
-  return await Promise.all(manifests.map(async manifest => {
-    const url = manifest.manifest_path
-    const entries = /** @type {ManifestEntry[]} */ (
-      await fetchAvroRecords(url, resolver, Number(manifest.manifest_length))
-    )
+async function fetchOneManifest(manifest, resolver) {
+  const url = manifest.manifest_path
+  const entries = /** @type {ManifestEntry[]} */ (
+    await fetchAvroRecords(url, resolver, Number(manifest.manifest_length))
+  )
 
-    // Inherit sequence number from manifest if not present in entry
-    for (const entry of entries) {
-      entry.partition_spec_id = manifest.partition_spec_id ?? 0
+  // Inherit sequence number from manifest if not present in entry
+  for (const entry of entries) {
+    entry.partition_spec_id = manifest.partition_spec_id ?? 0
 
+    if (entry.sequence_number === undefined) {
+      // When reading v1 manifests with no sequence number column,
+      // sequence numbers for all files must default to 0.
+      entry.sequence_number = manifest.sequence_number ?? 0n
+    }
+
+    if (entry.status === 1) {
+      // only ADDED can inherit sequence number
       if (entry.sequence_number === undefined) {
-        // When reading v1 manifests with no sequence number column,
-        // sequence numbers for all files must default to 0.
-        entry.sequence_number = manifest.sequence_number ?? 0n
+        entry.sequence_number = manifest.sequence_number
       }
-
-      if (entry.status === 1) {
-        // only ADDED can inherit sequence number
-        if (entry.sequence_number === undefined) {
-          entry.sequence_number = manifest.sequence_number
-        }
-        if (entry.file_sequence_number === undefined) {
-          entry.file_sequence_number = manifest.sequence_number
-        }
-      } else {
-        if (entry.sequence_number === undefined || entry.file_sequence_number === undefined) {
-          // spec violation "Sequence‑number inheritance"
-          throw new Error('iceberg manifest entry missing sequence number')
-        }
+      if (entry.file_sequence_number === undefined) {
+        entry.file_sequence_number = manifest.sequence_number
+      }
+    } else {
+      if (entry.sequence_number === undefined || entry.file_sequence_number === undefined) {
+        // spec violation "Sequence‑number inheritance"
+        throw new Error('iceberg manifest entry missing sequence number')
       }
     }
-    assignFirstRowIds(manifest, entries)
+  }
+  assignFirstRowIds(manifest, entries)
+
+  return { url, entries }
+}
 
-    return { url, entries }
-  }))
+/**
+ * Fetch manifest entries from a list of manifests, bounded to
+ * {@link MANIFEST_FETCH_CONCURRENCY} in-flight fetches. Result order matches
+ * the input `manifests` order.
+ *
+ * @param {Manifest[]} manifests
+ * @param {Resolver} resolver
+ * @returns {Promise<ManifestList>}
+ */
+async function fetchManifests(manifests, resolver) {
+  const results = /** @type {ManifestList} */ (new Array(manifests.length))
+  let next = 0
+  async function worker() {
+    while (next < manifests.length) {
+      const i = next++
+      results[i] = await fetchOneManifest(manifests[i], resolver)
+    }
+  }
+  const poolSize = Math.min(MANIFEST_FETCH_CONCURRENCY, manifests.length)
+  const workers = []
+  for (let w = 0; w < poolSize; w++) workers.push(worker())
+  await Promise.all(workers)
+  return results
 }
 
 /**
diff --git a/src/types.d.ts b/src/types.d.ts
index daf4cd3b829d2110545c977fa8c347b42a712722..1a338d40c55681960092857dd0c07166f999c167 100644
--- a/src/types.d.ts
+++ b/src/types.d.ts
@@ -177,7 +177,7 @@ interface PartitionStatistics {
   'file-size-in-bytes': bigint
 }
 
-interface SortOrder {
+export interface SortOrder {
   'order-id': number
   'fields': SortField[]
 }
diff --git a/src/write/stage.js b/src/write/stage.js
index 69491ce64e97d46177a6a4797346778803fe5918..8957cf635fac8242a08bf445600aa2708d6a6144 100644
--- a/src/write/stage.js
+++ b/src/write/stage.js
@@ -52,9 +52,11 @@ import { computeColumnStats } from './stats.js'
  * @param {Resolver} options.resolver - Resolver with a writer method.
  * @param {number} [options.sortOrderId] - Sort order id to apply; defaults to
  *   the table's `default-sort-order-id`.
+ * @param {bigint} [options.snapshotId] - Shared snapshot id for a multi-batch
+ *   append. Omit for the normal one-shot append.
  * @returns {Promise<PreparedAppend>}
  */
-export async function prepareAppend({ tableUrl, metadata, records, resolver, sortOrderId }) {
+export async function prepareAppend({ tableUrl, metadata, records, resolver, sortOrderId, snapshotId }) {
   if (!tableUrl) throw new Error('tableUrl is required')
   if (!resolver?.writer) throw new Error('resolver.writer is required')
   const writerFn = resolver.writer
@@ -73,7 +75,7 @@ export async function prepareAppend({ tableUrl, metadata, records, resolver, sor
   // commit, so reusing the id across attempts cannot corrupt anything. The
   // duplicate-snapshot-id check in applyUpdates (`commit.js`) guards against
   // colliding with a snapshot that was already committed by some other writer.
-  const snapshotId = newSnapshotId(metadata)
+  const appendSnapshotId = snapshotId ?? newSnapshotId(metadata)
   const manifestUuid = uuid4()
   checkWriteFormat(metadata.properties?.['write.format.default'])
   const codec = resolveParquetCodec(metadata.properties?.['write.parquet.compression-codec'])
@@ -126,7 +128,7 @@ export async function prepareAppend({ tableUrl, metadata, records, resolver, sor
     writer: manifestWriter,
     schema,
     partitionSpec,
-    snapshotId,
+    snapshotId: appendSnapshotId,
     dataFiles: writtenDataFiles.map(f => f.dataFile),
     formatVersion,
   })
@@ -140,7 +142,7 @@ export async function prepareAppend({ tableUrl, metadata, records, resolver, sor
   )
 
   return {
-    snapshotId,
+    snapshotId: appendSnapshotId,
     manifestUuid,
     formatVersion,
     manifestPath,
@@ -172,7 +174,7 @@ export async function prepareAppend({ tableUrl, metadata, records, resolver, sor
  * @param {Resolver} options.resolver
  * @returns {Promise<StagedUpdate>}
  */
-export async function stageSnapshotForAppend({ tableUrl, metadata, prepared, resolver }) {
+export async function stageSnapshotForAppend({ tableUrl, metadata, prepared, resolver, snapshotProperties }) {
   if (!tableUrl) throw new Error('tableUrl is required')
   if (!resolver?.writer) throw new Error('resolver.writer is required')
   const sequenceNumber = BigInt(metadata['last-sequence-number'] ?? 0) + 1n
@@ -215,6 +217,11 @@ export async function stageSnapshotForAppend({ tableUrl, metadata, prepared, res
     'total-delete-files': '0',
     'total-position-deletes': '0',
     'total-equality-deletes': '0',
+    // Caller-supplied extra summary properties (e.g. an idempotency token a
+    // commit-retry wrapper reads back to detect a landed-but-errored commit).
+    // Spread last; callers must namespace their keys so they never shadow the
+    // Iceberg-reserved counters above.
+    ...(snapshotProperties ?? {}),
   }
   return await buildSnapshotUpdate({
     tableUrl, metadata, resolver,
@@ -231,6 +238,88 @@ export async function stageSnapshotForAppend({ tableUrl, metadata, prepared, res
   })
 }
 
+/**
+ * Build one append snapshot from several independently prepared row batches.
+ * Each batch writes its parquet data and data manifest before this call, while
+ * all manifests share one snapshot id. The catalog sees one metadata commit.
+ *
+ * @param {object} options
+ * @param {string} options.tableUrl
+ * @param {TableMetadata} options.metadata
+ * @param {PreparedAppend[]} options.prepared
+ * @param {Resolver} options.resolver
+ * @param {Record<string, string>} [options.snapshotProperties]
+ * @returns {Promise<StagedUpdate>}
+ */
+export async function stageSnapshotForAppendBatches({ tableUrl, metadata, prepared, resolver, snapshotProperties }) {
+  if (!tableUrl) throw new Error('tableUrl is required')
+  if (!resolver?.writer) throw new Error('resolver.writer is required')
+  if (prepared.length === 0) throw new Error('prepared batches must not be empty')
+
+  const first = prepared[0]
+  for (const batch of prepared) {
+    if (batch.snapshotId !== first.snapshotId) throw new Error('prepared batches must share one snapshot id')
+    if (batch.formatVersion !== first.formatVersion) throw new Error('prepared batches must share one format version')
+  }
+
+  const sequenceNumber = BigInt(metadata['last-sequence-number'] ?? 0) + 1n
+  const timestampMs = Date.now()
+  const newManifests = prepared.map(batch => ({
+    manifest_path: batch.manifestPath,
+    manifest_length: batch.manifestLength,
+    partition_spec_id: batch.partitionSpecId,
+    content: /** @type {0} */ (0),
+    sequence_number: sequenceNumber,
+    min_sequence_number: sequenceNumber,
+    added_snapshot_id: first.snapshotId,
+    added_files_count: batch.addedDataFilesCount,
+    existing_files_count: 0,
+    deleted_files_count: 0,
+    added_rows_count: batch.addedRowCount,
+    existing_rows_count: 0n,
+    deleted_rows_count: 0n,
+    partitions: batch.partitions,
+  }))
+
+  const addedDataFilesCount = prepared.reduce((sum, batch) => sum + batch.addedDataFilesCount, 0)
+  const recordsCount = prepared.reduce((sum, batch) => sum + batch.recordsCount, 0)
+  const addedFilesSize = prepared.reduce((sum, batch) => sum + batch.addedFilesSize, 0n)
+  const prevSummary = currentSnapshot(metadata)?.summary
+  const prevTotals = {
+    records: BigInt(prevSummary?.['total-records'] ?? '0'),
+    size: BigInt(prevSummary?.['total-files-size'] ?? '0'),
+    files: BigInt(prevSummary?.['total-data-files'] ?? '0'),
+  }
+  const summary = {
+    operation: 'append',
+    'added-data-files': String(addedDataFilesCount),
+    'added-records': String(recordsCount),
+    'added-files-size': String(addedFilesSize),
+    'changed-partition-count': String(addedDataFilesCount),
+    'total-records': String(prevTotals.records + BigInt(recordsCount)),
+    'total-files-size': String(prevTotals.size + addedFilesSize),
+    'total-data-files': String(prevTotals.files + BigInt(addedDataFilesCount)),
+    'total-delete-files': '0',
+    'total-position-deletes': '0',
+    'total-equality-deletes': '0',
+    ...(snapshotProperties ?? {}),
+  }
+
+  return await buildSnapshotUpdate({
+    tableUrl,
+    metadata,
+    resolver,
+    snapshotId: first.snapshotId,
+    sequenceNumber,
+    manifestUuid: first.manifestUuid,
+    timestampMs,
+    formatVersion: first.formatVersion,
+    newManifests,
+    summary,
+    writtenFiles: [],
+  })
+}
+
 /**
  * Bundled prepare + stage. Equivalent to the prior single-call API; left in
  * place so callers that don't run inside a retry loop (e.g. `icebergTransaction`)
diff --git a/src/write/write.js b/src/write/write.js
index e136626b75d56e92d28baf1d0790686bae4b7d9f..ea0720ef00fe8008042b83e566ee0c90ce433a1a 100644
--- a/src/write/write.js
+++ b/src/write/write.js
@@ -5,7 +5,7 @@ import { loadLatestFileCatalogMetadata } from '../metadata.js'
 import { applyUpdates, fileCatalogCommit } from './commit.js'
 import { icebergStageDeletionVector } from './stage-deletion-vector.js'
 import { icebergStagePositionDelete } from './stage-position-delete.js'
-import { icebergStageAppend, icebergStageExpireSnapshots, icebergStageSetRef, icebergStageUpdateSchema, prepareAppend, stageSnapshotForAppend } from './stage.js'
+import { icebergStageAppend, icebergStageExpireSnapshots, icebergStageSetRef, icebergStageUpdateSchema, prepareAppend, stageSnapshotForAppend, stageSnapshotForAppendBatches } from './stage.js'
 import { icebergStageRewrite } from './rewrite.js'
 
 /**
@@ -34,7 +34,7 @@ const DEFAULT_RETRY = Object.freeze({
  * @param {number} [options.sortOrderId] - Sort order id to apply; defaults to the table default.
  * @returns {Promise<TableMetadata>}
  */
-export async function icebergAppend({ catalog, namespace, table, tableUrl, resolver, records, sortOrderId }) {
+export async function icebergAppend({ catalog, namespace, table, tableUrl, resolver, records, sortOrderId, snapshotProperties }) {
   const ctx = await loadTable({ catalog, namespace, table, tableUrl, resolver })
   // Spec v3 §"Manifest Inheritance": data and manifest files do NOT need to
   // be rewritten on optimistic-commit retry. Prepare them once outside the
@@ -55,6 +55,56 @@ export async function icebergAppend({ catalog, namespace, table, tableUrl, resol
       metadata: workingCtx.metadata,
       prepared,
       resolver: requireResolver(workingCtx.resolver, 'icebergAppend'),
+      snapshotProperties,
+    }),
+  })
+}
+
+/**
+ * Append a lazy sequence of bounded row batches as one Iceberg snapshot and
+ * one catalog commit. Data files and manifests are prepared sequentially, so
+ * the caller can release each decoded source chunk before yielding the next.
+ * Optimistic commit retries reuse the prepared files.
+ *
+ * @param {object} options
+ * @param {Catalog} options.catalog
+ * @param {string | string[]} [options.namespace]
+ * @param {string} [options.table]
+ * @param {string} [options.tableUrl]
+ * @param {Resolver} [options.resolver]
+ * @param {Iterable<Record<string, any>[]> | AsyncIterable<Record<string, any>[]>} options.batches
+ * @param {number} [options.sortOrderId]
+ * @param {Record<string, string>} [options.snapshotProperties]
+ * @returns {Promise<TableMetadata>}
+ */
+export async function icebergAppendBatches({ catalog, namespace, table, tableUrl, resolver, batches, sortOrderId, snapshotProperties }) {
+  const ctx = await loadTable({ catalog, namespace, table, tableUrl, resolver })
+  const writer = requireResolver(ctx.resolver, 'icebergAppendBatches')
+  const prepared = []
+  let snapshotId
+  for await (const records of batches) {
+    if (records.length === 0) continue
+    const batch = await prepareAppend({
+      tableUrl: ctx.tableUrl,
+      metadata: ctx.metadata,
+      records,
+      resolver: writer,
+      sortOrderId,
+      snapshotId,
+    })
+    snapshotId ??= batch.snapshotId
+    prepared.push(batch)
+  }
+  if (prepared.length === 0) return ctx.metadata
+
+  return await commitWithRetry({
+    catalog, target: { namespace, table }, ctx,
+    stage: workingCtx => stageSnapshotForAppendBatches({
+      tableUrl: workingCtx.tableUrl,
+      metadata: workingCtx.metadata,
+      prepared,
+      resolver: requireResolver(workingCtx.resolver, 'icebergAppendBatches'),
+      snapshotProperties,
     }),
   })
 }
diff --git a/types/index.d.ts b/types/index.d.ts
index 7cfd5378c18f0cef88a1cee7f397095dd75cce76..b1cb9f5da0ed074c5dd9c9b7124fd9448155a371 100644
--- a/types/index.d.ts
+++ b/types/index.d.ts
@@ -1,4 +1,4 @@
-export { IcebergTransactionConflictError, icebergAppend, icebergCreateTable, icebergDelete, icebergDropTable, icebergExpireSnapshots, icebergRewrite, icebergSetRef, icebergTransaction, icebergUpdateSchema } from './write/write.js';
+export { IcebergTransactionConflictError, icebergAppend, icebergAppendBatches, icebergCreateTable, icebergDelete, icebergDropTable, icebergExpireSnapshots, icebergRewrite, icebergSetRef, icebergTransaction, icebergUpdateSchema } from './write/write.js';
 export { icebergCreate } from './create.js';
 export { fileCatalog } from './catalog/file.js';
 export { restCatalogConnect, restCatalogCreateNamespace, restCatalogDropNamespace, restCatalogListNamespaces, restCatalogListTables, restCatalogLoadCredentials, restCatalogLoadTable, restCatalogRegisterTable, restCatalogRenameTable } from './catalog/rest.js';
diff --git a/types/manifest.d.ts b/types/manifest.d.ts
index 22ec9a36ff4df1f2d60c4ac7e9b4158bdf52df99..94c49dc420b70f09d742afb25524298d51f18cd3 100644
--- a/types/manifest.d.ts
+++ b/types/manifest.d.ts
@@ -1,8 +1,13 @@
-import type { Resolver, TableMetadata, ManifestEntry } from '../src/types.js';
+import type { Manifest, Resolver, TableMetadata, ManifestEntry } from '../src/types.js';
 export type ManifestList = {
     url: string;
     entries: ManifestEntry[];
 }[];
+/**
+ * Predicate applied to each manifest's manifest-list `partitions` field-summary
+ * array before entry fetch. Return `false` to skip the manifest's entry fetch.
+ */
+export type ManifestPartitionFilter = (partitions: Manifest['partitions'], partitionSpecId: number, manifest: Manifest) => boolean;
 /**
  * Returns manifest entries for a snapshot. Defaults to the current snapshot;
  * pass `snapshotId` to time-travel to a prior snapshot in the metadata's
@@ -16,10 +21,11 @@ export type ManifestList = {
  * @param {number | bigint} [options.snapshotId] - Optional snapshot id; defaults to `current-snapshot-id`.
  * @returns {Promise<ManifestList>}
  */
-export declare function icebergManifests({ metadata, resolver, snapshotId }: {
+export declare function icebergManifests({ metadata, resolver, snapshotId, partitionFilter }: {
     metadata: TableMetadata;
     resolver?: Resolver;
     snapshotId?: number | bigint;
+    partitionFilter?: ManifestPartitionFilter;
 }): Promise<ManifestList>;
 /**
  * Split manifest entries into data and delete manifests.
diff --git a/types/write/stage.d.ts b/types/write/stage.d.ts
index 3f29f9f3c0ab8cc742c4a57c8cc689fdc05d2dd4..2f92916a90b9edcbc5681f821beeeac85458cc89 100644
--- a/types/write/stage.d.ts
+++ b/types/write/stage.d.ts
@@ -40,12 +40,13 @@ import type { PreparedAppend, Resolver, Schema, StagedCommit, StagedUpdate, Tabl
  *   the table's `default-sort-order-id`.
  * @returns {Promise<PreparedAppend>}
  */
-export declare function prepareAppend({ tableUrl, metadata, records, resolver, sortOrderId }: {
+export declare function prepareAppend({ tableUrl, metadata, records, resolver, sortOrderId, snapshotId }: {
     tableUrl: string;
     metadata: TableMetadata;
     records: Record<string, any>[];
     resolver: Resolver;
     sortOrderId?: number;
+    snapshotId?: bigint;
 }): Promise<PreparedAppend>;
 /**
  * Build the snapshot, manifest list, and `StagedUpdate` for a previously
@@ -64,11 +65,19 @@ export declare function prepareAppend({ tableUrl, metadata, records, resolver, s
  * @param {Resolver} options.resolver
  * @returns {Promise<StagedUpdate>}
  */
-export declare function stageSnapshotForAppend({ tableUrl, metadata, prepared, resolver }: {
+export declare function stageSnapshotForAppend({ tableUrl, metadata, prepared, resolver, snapshotProperties }: {
     tableUrl: string;
     metadata: TableMetadata;
     prepared: PreparedAppend;
     resolver: Resolver;
+    snapshotProperties?: Record<string, string>;
+}): Promise<StagedUpdate>;
+export declare function stageSnapshotForAppendBatches({ tableUrl, metadata, prepared, resolver, snapshotProperties }: {
+    tableUrl: string;
+    metadata: TableMetadata;
+    prepared: PreparedAppend[];
+    resolver: Resolver;
+    snapshotProperties?: Record<string, string>;
 }): Promise<StagedUpdate>;
 /**
  * Bundled prepare + stage. Equivalent to the prior single-call API; left in
diff --git a/types/write/write.d.ts b/types/write/write.d.ts
index bcaab28249f2e0fdf05d0adacecd76b19f0311d5..f1aeb5af97b25900607fcd27fafe324170473a89 100644
--- a/types/write/write.d.ts
+++ b/types/write/write.d.ts
@@ -13,7 +13,7 @@ import type { Catalog, IcebergTransaction, Lister, PartitionSpec, Resolver, Sche
  * @param {number} [options.sortOrderId] - Sort order id to apply; defaults to the table default.
  * @returns {Promise<TableMetadata>}
  */
-export declare function icebergAppend({ catalog, namespace, table, tableUrl, resolver, records, sortOrderId }: {
+export declare function icebergAppend({ catalog, namespace, table, tableUrl, resolver, records, sortOrderId, snapshotProperties }: {
     catalog: Catalog;
     namespace?: string | string[];
     table?: string;
@@ -21,6 +21,18 @@ export declare function icebergAppend({ catalog, namespace, table, tableUrl, res
     resolver?: Resolver;
     records: Record<string, any>[];
     sortOrderId?: number;
+    snapshotProperties?: Record<string, string>;
+}): Promise<TableMetadata>;
+/** Append lazy row batches as one snapshot and one catalog commit. */
+export declare function icebergAppendBatches({ catalog, namespace, table, tableUrl, resolver, batches, sortOrderId, snapshotProperties }: {
+    catalog: Catalog;
+    namespace?: string | string[];
+    table?: string;
+    tableUrl?: string;
+    resolver?: Resolver;
+    batches: Iterable<Record<string, any>[]> | AsyncIterable<Record<string, any>[]>;
+    sortOrderId?: number;
+    snapshotProperties?: Record<string, string>;
 }): Promise<TableMetadata>;
 /**
  * Compact / rewrite a table in one call: load metadata, read every live row
